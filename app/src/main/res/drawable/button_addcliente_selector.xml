<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado quando o botão é pressionado -->
    <item android:drawable="@drawable/addcliente_pressed" android:state_pressed="true" />
    <!-- Estado padrão -->
    <item android:drawable="@drawable/addcliente" />
</selector>
